pipeline  {
    agent any 
    
    stages {
        
        stage ('Install Packages   SSH') {
            
            steps {
                sshagent (credentials: ['AWS-SSH-KEY']) {
                    sh """
                        ssh -o StrictHostKeyChecking=no  ec2-user@172.31.22.123 '
                        sudo yum install git -y; 
                        sudo yum install nginx -y;
                        sudo systemctl start nginx; 
                        cd ~; rm -rf fruits-veg_market ;
                        git clone https://github.com/ahmedwoye/fruits-veg-market.git;
                        cd fruits-veg_market; sudo cp web/index.html  /usr/share/nginx/html/ ' 
                    """
                }

            }
        }
    }
}

